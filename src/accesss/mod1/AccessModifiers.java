package accesss.mod1;

public class AccessModifiers {
	
	//Access Modifiers in Java ==> Set of keywords used to control the access of Java elements (classes, methods, variables)
	
	//4 types of Access Modifiers in Java:
	
	//1. public (Accessible from anywhere)
	//2. protected (Accessible within the same package and child classes in other packages)
	//3. default (no modifier) (Accessible only within the same package)
	//4. private (Accessible only within the same class)

}
